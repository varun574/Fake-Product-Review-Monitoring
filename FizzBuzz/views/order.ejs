<!DOCTYPE html>
<html>
  <head>
    <title>FizzBuzz</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body class="container-fluid">
  <%- include('header')-%>
    <div class="card">
      <% if(order.status==='ORDER_SUCCESSFUL'){%>
        <div class="card-header text-success">
            <h3>Order Placed</h3>
        </div>
      <%}else if(order.status ==='ORDER_UNSUCCESSFUL'){%>
        <div class="card-header text-danger">
          <h3>Order Unsuccessful</h3>
        </div>
      <%}else{%>
        <div class="card-header text-info">
          <h3><%= order.status%></h3>
        </div>
      <%}%>
            <div class="card-body">
                <h4 style="display: inline;">Product </h4> : <%= order.product%><br>
                <h4 style="display: inline;">Amount </h4> : <%= order.price%>₹<br>
                <h4 style="display: inline;">Ordered By </h4> : <%= order.customer%><br>
                <h4 style="display: inline;">Address </h4> : <%= order.address %><br>
            </div>
        <div class="card-footer">
            <h6 ><%= new Date(order.date) %></h6>
        </div>
    </div>
  <%- include('footer') -%>
  </body>
</html>