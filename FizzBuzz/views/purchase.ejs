<!DOCTYPE html>
<html>
  <head>
    <title>FizzBuzz</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
  <%- include('header')-%>
  <div class="row">
      <div class="col-1"></div>
        <div class="col-3">
            <!-- <img src="<%= product.image%>" onerror="this.onerror=null; this.src='/images/defualtproduct.jpeg'" alt="Image unavailable" width="300px" height="250px"> -->
        </div>  
        <div class="col-7">
            <h3 style="display: inline;">Product</h3> :  <%= product.product_name%><br>
            <h4 style="display: inline;">Price </h4> : <%= product.price%>₹<br>
            <% if(message.length!=0){%>
                <div class="alert alert-danger alert-dismissible fade show m-3">
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    <strong><%= message[0] %></strong>
                </div>    
            <%}%>
            <form method="post" action="/purchase/<%= product.product_name%>">
                <input type="hidden" name="name" value="<%= username%>">
                <label for="address" class="form-label">Address</label>
                <input type="text" name="address" id="address" class="form-control" value="<%= address%>">
                <label for="mobile" class="form-label">Mobile</label>
                <input type="text" name="mobile" id=",mobile" class="form-control" value="<%= mobile%>">
                <input type="submit" class="btn btn-success" value="Place order">
            </form>

        </div>
        <div class="col-1"></div>
  </div>
  <%- include('footer') -%>
  </body>
</html>